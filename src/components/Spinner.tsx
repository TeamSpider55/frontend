import { CircularProgress } from '@mui/material';
import React from 'react';

const Spinner = () => (
  <CircularProgress
    size="1.5rem"
    sx={{ color: 'white' }}
  />
);

export default Spinner;
